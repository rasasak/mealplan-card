{
  "name": "mealplan-card",
  "version": "1.0.0",
  "main": "dist/mealplan-card.js",
  "description": "MealPlan Card for Home Assistant",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run --coverage --reporter=verbose",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "version:update": "node scripts/update-version.js",
    "build": "vite build",
    "supported": "node scripts/generate-manufacturer-list.mjs > SUPPORTED.md",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit && eslint . && prettier --check .",
    "lint:fix": "npx tsc --noEmit && eslint --fix . && prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@open-wc/testing": "^4.0.0",
    "@types/eslint__js": "^9.14.0",
    "@vitest/coverage-v8": "^4.1.3",
    "eslint": "^10.2.0",
    "husky": "^9.1.7",
    "jsdom": "^29.0.2",
    "prettier": "^3.8.1",
    "terser": "^5.46.1",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.58.0",
    "vite": "^8.0.6",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "^4.0.8"
  },
  "dependencies": {
    "@kipk/load-ha-components": "^1.0.3",
    "@open-wc/semantic-dom-diff": "^0.20.1"
  }
}
